# URL Clean

Very simple url cleaning script

## Options

* `list-update` - Update all lists
* `clean-url` - Clean up any url with tracking Parameters
* `fetch-url-query-params` - Fetch the query parameters of the actual url
* `fetch-true-url` - Fetch the actual url
* `clipboard-watchers` - Start clipboard watchers